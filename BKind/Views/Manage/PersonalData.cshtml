@model BKind.Models.Manage.PersonalData
@{
    ViewData["Title"] = "Date personale";
    ViewData["ActivePage"] = BKind.Models.Manage.ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-6">
        <p>Contul tău conține date personale, pe care tu le-ai furnizat. Această pagină îți permite să descarci sau să ștergi aceste date.</p>
        <p>
            <strong>Eliminarea acestor date va șterge permanent contul tău, fără a mai putea fi recuperat.</strong>
        </p>
        <form asp-action="DownloadPersonalData" id="download-data" method="post" class="form-group">
            <button class="btn btn-outline-dark" type="submit">Descarcă</button>
        </form>
        <p>
            <a asp-action="DeletePersonalData" id="delete" class="btn btn-outline-dark">Șterge</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}